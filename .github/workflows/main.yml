name: Docker Image Downloader
on: workflow_dispatch
jobs:
  download:
    runs-on: ubuntu-latest
    steps:
      - name: Pull Image
        run: docker pull quay.io/ascend/vllm-ascend:v0.14.0rc1
      
      - name: Save Image to Tar
        run: docker save quay.io/ascend/vllm-ascend:v0.14.0rc1 -o vllm-ascend-0.14.0rc1.tar
      
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: vllm-ascend-image
          path: vllm-ascend-0.14.0rc1.tar
          retention-days: 1
